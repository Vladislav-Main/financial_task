import React from "react";
import "./infoMessage.scss";

const InfoMessage = () => {
  return (
    <h1 className="message">
      Please start the server for open the app: Use yarn dev
    </h1>
  );
};

export default InfoMessage;
